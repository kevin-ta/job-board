<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    {{ form_rest(form) }}
	</br>
    <div class="form-group">
      	<div class="col-lg-10">
        	<input type="submit" id="_submit" name="_submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" class="btn btn-default" />
        </div>
    </div>
</form>